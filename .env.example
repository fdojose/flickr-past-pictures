# Copied to .env on casa.local (docker compose reads it). The webhook id must match
# homeassistant/flickr.yaml (deployed as /config/packages/flickr.yaml).
HA_WEBHOOK_URL=http://127.0.0.1:8123/api/webhook/flickr-REPLACE_ME
